# Dependencias
node_modules/

# Variables de entorno (contiene credenciales de Supabase — NUNCA subir)
.env
.env.*
!.env.example

# Artefactos de build (la app se distribuye como PWA desde el navegador)
dist/

# CSS compilado — se incluye en el repo para produccion (Render no lo compila)
# public/css/output.css

# Logs
*.log
npm-debug.log*

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Temporales
tmp/
temp/
